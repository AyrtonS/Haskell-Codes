---------------------------------------------------------------------------------------------------------------------
-- \ x -> 2*x + 1
--f = \x -> 2*x + 1
somaPar = \(x,y) -> x + y
---------------------------------------------------------------------------------------------------------------------

---------------------------------------------------------------------------------------------------------------------
--fatorial = \n ->  product [1..n]
--(\x y -> sqrt(x*x + y*y)) 3 4
--(\xs -> let n = div(length xs) 2 in (take n xs,drop n xs)) "Bom dia" ~> ("Bom","dia")
--(\(x1,y1) (x2,y2) -> sqrt((x2-x1)^2 + (y2-y1)^2)) (6,7) (9,11)
---------------------------------------------------------------------------------------------------------------------

---------------------------------------------------------------------------------------------------------------------
--impares n = map f[0..n-1]
--  where f x = x*2 + 1
---------------------------------------------------------------------------------------------------------------------
dobraValor :: (Int->Int)->Int->Int
dobraValor f n = f(f n)
---------------------------------------------------------------------------------------------------------------------
proxNum :: (Int -> Int)
proxNum = \x -> x+1
---------------------------------------------------------------------------------------------------------------------
composta :: (Float->Float)->(Float->Float)->(Float->Float)
composta f g = f.g
